{
	"manifest_version": "0.4",
	"name": "babylonjs-editor",
	"display_name": "Babylon.js Editor",
	"version": "1.0.0",
	"description": "Control the Babylon.js Editor to build games and 3D applications: compose scenes from primitives and assets, manage materials/textures, lights and cameras, download marketplace assets, write and attach TypeScript behaviors, optimize with mesh instancing, and verify the result visually with screenshots.",
	"long_description": "This extension exposes the Babylon.js Editor's scene-composition capabilities to Claude so you can build real, hand-editable WebGL games and 3D scenes from natural language. Everything the agent creates (meshes, materials, instances, lights, cameras, particles, physics) becomes genuine editor content you can keep tweaking by hand.\n\n**Requirements:** the Babylon.js Editor (version 5+) must be running with a project open. The extension's MCP server talks to the editor over a local HTTP channel (127.0.0.1:3712); it does not modify any files on its own and performs no network access beyond the local editor.\n\nTypical uses: scatter a forest from a single tree, lay out a level from modular pieces, set up sky/lighting/post-processing, author materials and physics bodies, and attach runtime behavior scripts — all verifiable live with in-editor screenshots.",
	"author": {
		"name": "Julien Moreau-Mathis",
		"url": "https://github.com/BabylonJS/Editor"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/BabylonJS/Editor"
	},
	"homepage": "https://editor.babylonjs.com",
	"documentation": "https://editor.babylonjs.com/documentation",
	"support": "https://github.com/BabylonJS/Editor/issues",
	"license": "Apache-2.0",
	"keywords": ["babylonjs", "babylon", "3d", "webgl", "game-engine", "editor", "scene", "gamedev", "mcp"],
	"server": {
		"type": "node",
		"entry_point": "server/index.mjs",
		"mcp_config": {
			"command": "node",
			"args": ["${__dirname}/server/index.mjs"]
		}
	},
	"tools_generated": false,
	"tools": [
		{ "name": "get_scene_hierarchy", "description": "Get the current scene's node hierarchy as a tree of { id, name, type, children }." },
		{ "name": "list_scenes", "description": "List all .scene assets in the project and which one is active." },
		{ "name": "get_active_scene", "description": "Get the active scene's name/path plus mesh, light and material counts." },
		{ "name": "save_scene", "description": "Save the current scene/project to disk." },
		{ "name": "get_scene_settings", "description": "Get scene-level settings (clear/ambient color, environment texture, fog, active camera)." },
		{ "name": "set_scene_settings", "description": "Set scene-level settings via dotted property paths." },
		{ "name": "get_node", "description": "Get full details of a node by id or name." },
		{ "name": "set_node_transform", "description": "Set a node's position/rotation/scaling (centimeters, radians)." },
		{ "name": "set_node_properties", "description": "Deep-set arbitrary node properties by dotted path." },
		{ "name": "set_node_parent", "description": "Reparent a node, preserving its world transform." },
		{ "name": "rename_node", "description": "Rename a node." },
		{ "name": "delete_node", "description": "Delete a node and its descendants." },
		{ "name": "select_node", "description": "Select/focus a node in the editor (UX only)." },
		{
			"name": "get_selected_nodes",
			"description": "Get the nodes the user has currently selected in the editor's scene graph (e.g. to instantiate the selected node to build a forest)."
		},
		{ "name": "create_primitive_mesh", "description": "Create a primitive mesh (box, sphere, ground, plane, cylinder, etc.) or an empty transform node." },
		{ "name": "create_instance", "description": "Create InstancedMesh copies of a source mesh (performance-preferred over cloning)." },
		{ "name": "clone_mesh", "description": "Clone a mesh; share geometry by default. Use only when a copy needs a different material." },
		{ "name": "set_mesh_material", "description": "Assign an existing material to a mesh." },
		{ "name": "set_mesh_visibility", "description": "Toggle a mesh's visible/enabled state or set its visibility factor." },
		{ "name": "set_mesh_physics", "description": "Add/update/remove a Havok physics body on a mesh (mass, motion type, shape, friction, restitution) for gameplay." },
		{ "name": "get_mesh_bounding_info", "description": "Get a mesh's local/world bounding box and hierarchy bounds (size, min, max, center) for scattering and placement." },
		{ "name": "create_light", "description": "Create a directional, point, spot or hemispheric light." },
		{ "name": "set_light_shadows", "description": "Enable and configure a light's shadow generator (classic or cascaded for directional lights)." },
		{ "name": "remove_light_shadows", "description": "Remove the shadow generator from a light so it stops casting shadows." },
		{ "name": "create_clustered_light_container", "description": "Create the scene's ClusteredLightContainer for non-shadow lights." },
		{ "name": "add_light_to_clustered_container", "description": "Move a non-shadow light into the ClusteredLightContainer for performance." },
		{ "name": "remove_light_from_clustered_container", "description": "Remove a light from the ClusteredLightContainer; it returns to the scene as a regular light." },
		{ "name": "create_camera", "description": "Create a free, arcrotate or universal camera." },
		{ "name": "set_active_camera", "description": "Set the scene's active camera." },
		{ "name": "get_camera_post_processes", "description": "Read a camera's post-processes (bloom, tone mapping, SSAO, SSR, motion blur, VLS, TAA)." },
		{
			"name": "set_camera_post_process",
			"description": "Enable/disable and customize a per-camera post-process in realtime (switches the active camera so it is set up for runtime)."
		},
		{ "name": "list_materials", "description": "List all materials in the scene/project." },
		{ "name": "list_material_types", "description": "List creatable material types incl. the Materials Library (sky, water, lava, grid, ...) with their key properties." },
		{ "name": "create_material", "description": "Create a material (PBR, Standard, Node, or a Materials Library type like SkyMaterial) and persist it as a .material asset." },
		{ "name": "set_material_properties", "description": "Deep-set material properties by dotted path." },
		{ "name": "assign_texture_to_material", "description": "Load a texture asset and assign it to a material channel." },
		{ "name": "set_environment_texture", "description": "Set the scene environment/skybox from a cube texture asset." },
		{ "name": "list_assets", "description": "List project assets, optionally filtered by type/folder, with preview availability." },
		{ "name": "get_asset_preview", "description": "Return an asset/folder preview image as base64 (image)." },
		{ "name": "instantiate_mesh_asset", "description": "Load a mesh asset into the scene (drag'n'drop equivalent; glTF auto-scaled)." },
		{ "name": "list_particle_assets", "description": "List the .npss node particle system assets in the project." },
		{ "name": "instantiate_particle_system", "description": "Instantiate a particle system asset or create a default one in the scene." },
		{ "name": "list_sound_assets", "description": "List the .mp3/.ogg/.wav sound assets available in the project." },
		{ "name": "create_sound", "description": "Create a SoundNode from a sound asset; spatial (3D positional) or global 2D ambience/music." },
		{ "name": "set_sound_properties", "description": "Update an existing SoundNode's volume, spatial mode, falloff and panning properties." },
		{ "name": "list_animation_groups", "description": "List the scene's animation groups (imported clips and authored ones)." },
		{ "name": "play_animation_group", "description": "Preview an animation group in the editor (loop, speed, frame range)." },
		{ "name": "stop_animation_group", "description": "Stop a playing animation group, or all animation groups if none is given." },
		{ "name": "create_animation", "description": "Author a keyframe AnimationGroup on a node property (e.g. a door opening) for behavior scripts to play at runtime." },
		{ "name": "delete_animation_group", "description": "Remove and dispose an animation group from the scene by name." },
		{ "name": "open_marketplace", "description": "Open the visible marketplace browser tab in the editor." },
		{ "name": "search_marketplace", "description": "Search a marketplace through the editor's visible browser." },
		{ "name": "download_marketplace_asset", "description": "Trigger a visible download of a marketplace asset into the project." },
		{ "name": "list_scripts", "description": "List TypeScript scripts under the project's src/." },
		{ "name": "create_script", "description": "Create a new TS script with the editor's default skeleton under src/." },
		{ "name": "read_script", "description": "Read a script file's content." },
		{ "name": "write_script", "description": "Overwrite/update a script file's content." },
		{ "name": "attach_script", "description": "Attach a script file to a node." },
		{ "name": "list_attached_scripts", "description": "List scripts attached to a node and their exported values." },
		{ "name": "set_script_exported_value", "description": "Set an exported/inspector value of a script attached to a node." },
		{ "name": "detach_script", "description": "Detach a script from a node." },
		{ "name": "get_editor_api", "description": "Describe the `editor` mediator object available to agent automation scripts." },
		{ "name": "write_agent_script", "description": "Write a .js automation script into the project's agentdata/ folder (export function main(editor))." },
		{ "name": "run_agent_script", "description": "Compile and run an agentdata/ automation script via main(editor) for complex/procedural tasks." },
		{ "name": "list_agent_scripts", "description": "List the .js automation scripts in the project's agentdata/ folder." },
		{ "name": "get_screenshot", "description": "Capture a preview screenshot for visual verification (image)." },
		{ "name": "focus_node", "description": "Frame the editor camera on a node." },
		{ "name": "run_project", "description": "Start the project's dev/run process." },
		{ "name": "execute_batch", "description": "Execute an ordered list of { tool, arguments } actions in one round-trip." }
	],
	"compatibility": {
		"platforms": ["darwin", "win32", "linux"],
		"runtimes": { "node": ">=20.0.0" }
	}
}
