import { CompilerOptions } from "typescript";

export const compilerOptions: Partial<CompilerOptions> = {
	// To be filled.
}

export const aliases: Record<string, string[]> = {
	// To be filled.
};

export const workspaceConfiguration = {
	dirPath: "",
};
