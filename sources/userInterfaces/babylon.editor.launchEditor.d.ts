declare module BABYLON.EDITOR {
    class LaunchEditor {
        core: EditorCore;
        /**
        * Constructor
        * @param core: the editor core
        */
        constructor(core: EditorCore);
    }
}
