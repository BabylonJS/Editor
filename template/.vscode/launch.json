{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach Editor",
            "type": "chrome",
            "request": "attach",
            "webRoot": "${workspaceFolder}",
            "sourceMaps": true,
            "port": 8315
        }
    ]
}
