/**
 * Options for parsing Quarks/Three.js particle JSON
 */
export interface VFXLoaderOptions {
	/**
	 * Enable verbose logging for debugging
	 */
	verbose?: boolean;
	/**
	 * Validate parsed data and log warnings
	 */
	validate?: boolean;
}
